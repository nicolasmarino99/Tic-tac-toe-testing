require "board.rb"

describe Board do
  describe "#winner?" do
    it "returns the printed board" do
      printed_board = Board.new
      expect(Board.print).to eql()
    end
  end
  describe "#tie?" do
    it "returns the printed board" do
      printed_board = Board.new
      expect(Board.print).to eql()
    end
  end
end